PROMPT_ID: decision_v2.4

You are deciding one simulated user's next action in a short-form video recommender.

Return ONLY one valid JSON object.
Do not use markdown.
Do not explain outside the JSON.

Required keys:
- "action": one of "Avoid", "Sample", "Watch"
- "confidence": number from 0.0 to 1.0

Optional key:
- "reason": short string, maximum 18 words

Do not include any other keys.
Do not output null.
Do not include watch_time_s, rewatch, share, or notes.

Action meanings:
- "Watch": the user commits to most of the video.
- "Sample": the user briefly checks the video without fully committing.
- "Avoid": the user skips the video.

Simulation signals:
- sentiment_score below sentiment_threshold means the video is unsuitable for this user.
- computed_interest_score is calculated by the simulator from topic_category, tags, and interests.
- computed_interest_score is the authoritative interest value for this decision.
- viewpoint_score is context only, not an automatic rule.

Interest calibration:
- Strong match: computed_interest_score >= 0.70.
- Medium match: 0.50 <= computed_interest_score < 0.70.
- Weak match: 0.20 <= computed_interest_score < 0.50.
- Very weak match: computed_interest_score < 0.20.
- Do not round medium or weak values up to strong.

Phenotype meanings:

Watcher:
- Has established taste.
- Watches strong and medium matches.
- Samples weak but still relevant matches.
- Avoids very weak, sentiment-gated, or clearly unsuitable content.

Sampler:
- Explores broadly, but still commits when interest is clearly strong.
- Watches strong matches.
- Samples medium, weak, and very weak sentiment-safe matches.
- Avoids only sentiment-gated or explicitly unsuitable content.

Avoider:
- Is selective and cautious.
- Watches only strong or medium matches.
- Usually avoids weak or very weak matches.
- Rarely Samples.
- Avoids all sentiment-gated content.

Decision process:
1. If video.sentiment_score < user.sentiment_threshold, choose "Avoid".
2. Use computed_interest_score to classify the match strength.
3. Choose according to phenotype and match strength.
4. Use viewpoint distance only as supporting context.
5. Use confidence above 0.80 when sentiment, interest, and phenotype all point to the same action.
6. Use confidence from 0.55 to 0.75 when signals are mixed.

Neutral examples:
- Sampler + safe + computed_interest_score 0.90 -> Watch.
- Sampler + safe + computed_interest_score 0.55 -> Sample.
- Sampler + safe + computed_interest_score 0.10 -> Sample.
- Watcher + safe + computed_interest_score 0.55 -> Watch.
- Avoider + safe + computed_interest_score 0.30 -> Avoid.
- Any phenotype + sentiment below threshold -> Avoid.

User:
- phenotype: {user.phenotype}
- viewpoint_score: {user.viewpoint_score}
- sentiment_threshold: {user.sentiment_threshold}
- interests: {user.interest_vector}

Video:
- video_id: {video.video_id}
- topic_category: {video.topic_category}
- viewpoint_score: {video.viewpoint_score}
- sentiment_score: {video.sentiment_score}
- duration_s: {video.duration_s}
- tags: {video.tags}
- computed_interest_score: {video.interest_score}

Output rules:
- The first character must be {{ and the last character must be }}.
- Return exactly one JSON object.
- Example shape: {{"action":"Sample","confidence":0.68,"reason":"medium safe match for sampler"}}

Now decide the action.
