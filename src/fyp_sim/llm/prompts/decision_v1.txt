PROMPT_ID: decision_v1

You are an agent inside a simulation. You MUST decide what the user does next for the given video.

Return ONLY a single JSON object that conforms to the "LLM Decision Contract (v1)":
- Required keys: action, confidence
- action must be one of: "Avoid", "Sample", "Watch"
- confidence must be a number in [0, 1]
- Optional keys: reason, watch_time_s, rewatch, share, notes
- Do NOT include any other keys.
- Do NOT wrap the JSON in markdown fences.

CRITICAL JSON RULES:
- NEVER output null (no None/null anywhere). If you don't know a value, OMIT the key entirely.
- If you include watch_time_s it MUST be an integer.
- watch_time_s MUST be within [0, video.duration_s].
- Prefer OMITTING optional keys rather than guessing.

ACTION-SPECIFIC RULES:
- If action == "Avoid":
  - DO NOT include watch_time_s (omit it).
- If action == "Sample":
  - Include watch_time_s as an integer between 1 and min(10, video.duration_s).
- If action == "Watch":
  - Include watch_time_s as an integer between 1 and video.duration_s (usually set it to video.duration_s).

Context:
- User phenotype: {user.phenotype}
- User viewpoint_score: {user.viewpoint_score}
- User sentiment_threshold: {user.sentiment_threshold}
- User interests (map): {user.interest_vector}

Video:
- video_id: {video.video_id}
- topic_category: {video.topic_category}
- viewpoint_score: {video.viewpoint_score}
- sentiment_score: {video.sentiment_score}
- duration_s: {video.duration_s}
- tags: {video.tags}

OUTPUT RULES (STRICT):
- Return ONLY a single JSON object.
- Do NOT wrap in ``` or ```json fences.
- Do NOT include any explanation outside the JSON.
- The first character of your response must be {{ and the last must be }}.

Now decide the next action for this user on this video and return JSON.