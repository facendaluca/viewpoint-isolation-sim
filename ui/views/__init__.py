"""
Dashboard pages for the Streamlit UI.

These modules should remain UI-only:
- Rendering, navigation, input widgets, and displaying results
- No simulation logic (that belongs in src/ and is called via backend API)
"""
